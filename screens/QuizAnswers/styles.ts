import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#000000',
    },
    scrollContainer: {
        flex: 1,
    },
    contentHeader: {
        padding: 16,
        paddingTop: 24,
    },
    subtitle: {
        fontSize: 24,
        fontWeight: 'bold',
        color: '#FFFFFF',
        marginBottom: 16,
    },
    answersContainer: {
        padding: 16,
    },
}); 